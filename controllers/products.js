//Importamos el modelo
const Product = require('../models/Product');

//Funcionalidades
function mostrarArticulos(req, res){};

function buscarArticulos(req, res){};

function publicarArticulo(req, res){};

function actualizarArticulos(req, res){};

function eliminarArticulos(req, res){};

function verArticulosPorUsuario(req, res){};

module.exports = {
    mostrarArticulos,
    buscarArticulos,
    publicarArticulo,
    actualizarArticulos,
    eliminarArticulos,
    verArticulosPorUsuario
};